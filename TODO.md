Items on the to-do list :
* Secrets Manager / SSM Parameter Store
* Auto Scaling Groups
* HTTPS (ACM + ALB)
* Check output from 'kubectl get secret my-secret -o yaml'


Current Status :
* [ DONE ] SSH into instance → confirm it works
* [ DONE ] Add user_data scripts
  * Need to manage wp archive recovery
* [ DONE ] EFS
* ALB
* SG linking cleanup
